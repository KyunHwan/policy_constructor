# `examples/`

- `examples/build_and_run.py`: builds and runs an example model in this repo
- `examples/end_to_end.md`: walkthrough for using `model_constructor` as a submodule in training/inference repositories


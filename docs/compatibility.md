# Compatibility

- Python: 3.10+
- PyTorch: >= 2.2

The builder performs a small runtime check and raises a clear error if the torch version is too old.

See `docs/README.md` for the full documentation index.

# `configs/`

This folder contains example YAML configurations used to exercise the constructor.

- `configs/examples/sequential_mlp.yaml`: demonstrates the `model.sequential` frontend
- `configs/examples/graph_skip_add.yaml`: demonstrates the `model.graph` frontend with a skip/add

For the full YAML contract, see `model_constructor/config/schema_v1.md`.

